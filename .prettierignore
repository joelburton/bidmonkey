# Files whose exact layout is either generated or load-bearing, so prettier must
# not reflow them.
dist/
package-lock.json

# Generated: the base64 DejaVu subset the quiz PDF embeds.
src/lib/quizPdfFont.ts

# Authored content, not code: the quiz YAML's comments and block layout carry
# meaning (book labels, per-problem notes) and normalize_quiz.py is the only
# thing that should have opinions about it.
problems/

# Migrations are the schema's source of truth; seed.sql is generated by
# db/gen-seed.mjs.
supabase/

# Hand-wrapped prose (CLAUDE.md especially) — reflowing it would churn every
# paragraph for no gain.
*.md
