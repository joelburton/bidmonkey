# OS
.DS_Store

# Built SQLite database (reproducible: sqlite3 bidmonkey.db < schema.sql < seed.sql)
*.db

# Dependencies / build output (also covered by web/.gitignore)
node_modules/
dist/
*.log

# Playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Claude Code local settings
.claude/settings.local.json
